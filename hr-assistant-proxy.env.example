# Copy to your shell env before starting the proxy.
# Only set the providers you actually use.

PORT=8787
# Optional: if set, the frontend must send this as Bearer token.
HR_PROXY_TOKEN=

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
DEEPSEEK_API_KEY=
KIMI_API_KEY=
